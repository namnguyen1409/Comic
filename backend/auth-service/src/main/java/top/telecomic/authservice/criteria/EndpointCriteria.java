package top.telecomic.authservice.criteria;

import top.telecomic.authservice.dto.filter.endpoint.EndpointFilter;
import top.telecomic.authservice.entity.Endpoint;

public interface EndpointCriteria extends CriteriaHelper<Endpoint, EndpointFilter> {
}
